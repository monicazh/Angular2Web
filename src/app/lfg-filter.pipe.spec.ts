import { LfgFilterPipe } from './lfg-filter.pipe';

describe('LfgFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LfgFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
